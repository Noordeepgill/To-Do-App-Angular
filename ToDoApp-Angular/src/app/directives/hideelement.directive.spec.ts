import { HideelementDirective } from './hideelement.directive';

describe('HideelementDirective', () => {
  it('should create an instance', () => {
    const directive = new HideelementDirective();
    expect(directive).toBeTruthy();
  });
});
